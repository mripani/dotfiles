# Extra autostart processes
# exec-once = uwsm-app -- my-service

# Fix stale input regions when workspaces migrate on monitor disconnect
exec-once = ~/.config/hypr/scripts/monitor_disconnect/target/release/monitor-disconnect
